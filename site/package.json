{
  "name": "kino-site",
  "version": "0.0.0",
  "private": true,
  "description": "Kino documentation and landing page (Cloudflare Worker, static assets only)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "wrangler": "^3.99.0"
  }
}
